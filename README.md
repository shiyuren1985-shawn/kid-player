# kid player · Android 原型

当前私人仓库：`https://github.com/shiyuren1985-shawn/kid-player`。Mini 源码目录：`/Volumes/ZHITAI/AI/KidPlayer/kid-player`；新增环境和缓存位于相邻外置盘 `runtime/`，Python 虚拟环境位于 `tools/.venv`。开发只在 Mini 单端进行。

用于观看家长指定范围的视频。一个 APK 包含触控平板布局与电视遥控器布局，保留本地演示与 SMB，并新增实验性 B 站指定 UP 主订阅。

## 0.6.1 缓存与 UP 主入口（2026-09-07）

- 已保存的作者目录优先显示；读取云端名单或更新目录时，仍可浏览已有影片。目录联网复核成功后替换，失败保留上次内容。
- 头像和封面按完整图片地址缓存，相同地址复用，新增或变化的地址才下载。沿用已有磁盘缓存，不再每隔 7 天重下；同地址并发请求合并，损坏缓存会重取，下载内容通过图片解码校验才写入。内存上限 12 MiB，磁盘上限 24 MB，超出时淘汰较久未读取的图片；系统清缓存或淘汰后会重新下载。
- 本机制缓存目录、头像和封面，不下载完整视频；目录元数据仍联网核验新增、下架和权限变化。同地址原地替换的图片按地址缓存，需缓存被清理后才能重新获取。
- 刷新按钮旁显示转圈，完成、失败或取消后消失；不显示无法可靠计算的百分比。
- 在线首页的“UP 主订阅”改为“管理 UP 主”，直接打开名单管理，不会自动取消或隐藏作者；其他来源下显示“UP 主视频”用于切换。名单内的启用开关才决定是否显示该作者。

验证记录见 [0.6.1 验证](docs/CACHE-0.6.1-VERIFICATION.md)。

## 0.6 应用内更新（2026-09-07）

当前版本 `0.6.1` / versionCode 10。更新源为 [kid-player.shiyu.ren](https://kid-player.shiyu.ren/kid-player/update.json)，经 Cloudflare Tunnel 连接 Mac Mini 外置盘的只读发布目录。

首页自动检查新版本，后台约15分钟申请检查并在允许通知时提示；系统可能延后。也可在“播放设置 → 检查应用更新”手动检查。支持版本说明、下载进度、取消重试、大小/SHA256/包名/版本/签名校验及系统安装授权，播放中不弹更新提示。本版仍需用户确认安装，不承诺静默更新。首次装入兼容签名的更新版后，后续无需电脑手工拷贝 APK。

电脑发布：增加版本号，完成相关验证后运行 `bash tools/publish-update.sh --notes '更新说明'`。脚本先放版本化 APK，再原子切换清单。完整使用、签名限制、服务和未来 GitHub 来源迁移见 [应用更新说明](docs/UPDATES.md)。

## 0.5 点开就播与常驻按钮区（2026-09-05）

点影片卡片直接进入播放，移除播放前的详情确认弹窗。视频下方常驻按钮区包含“返回列表、播放/暂停、收藏、关闭影院”，按钮最小高度 72dp；“关闭影院”停止播放并退出当前 App 任务，“返回列表”回到原来的浏览位置。按钮区独立于画面，不随视频控制器自动隐藏。窄屏使用两列按钮，宽屏使用四列；焦点使用深紫边框。

播放结束显示“再看一遍”，在线影片同时展示最多四部同一 UP 主的其他影片，由孩子点击选择。候选仅来自当前批准目录，排除刚看完的影片，优先显示已有观看进度的故事；不会自动开始下一部，也不会引入其他作者或平台的个性化推荐。点击候选时再次核对当前作者和目录。返回结束页不会擅自重播，重放从零开始。

网络/解码错误显示在视频区域，常驻按钮仍可使用。播放中收藏立即保存，回列表后可在“我的收藏”找到。云端名单、公开合集最新 30 条范围、原有数据和无 PIN 设定延续 0.4。

当时版本 `0.5.0-easy-player` / versionCode 6。操作参考了 YouTube 的[片尾视频选择与自动播放说明](https://support.google.com/youtube/answer/6327615?hl=en-GB)，本应用采用手动选择同作者影片。

Air 历史验证（不代表 Mini 本次验收）：13 项单元测试通过；完整模拟器回归 49 项中 47 项通过、2 项在线首播超时，这两项随后在同一 APK 上单独重测通过。遥控器暂停后的焦点、真实同作者切换、三部视频的音视频解码/跳转/暂停恢复、离线重试和 SMB 均有通过记录。在线首播偶发超时的根因尚未确认。检查记录见 `qa/sisi-0.5.0/development-review.md`、`verification.json` 和 `visual-review.md`；APK 保存在 `qa/releases/0.5.0-easy-player/`。

下面的 0.4 及更早内容为历史记录，与上面的当前交互不同时以 0.5 为准。

## 0.4 云端名单与kid player（2026-09-05）

当前统一名称为“kid player”，奶油白、莓果粉和淡紫配色，保留爱心萌可图标。默认由你自己的 HTTPS JSON 文件管理 UP 主名单；App 只配置文件地址，云端模式下不能在本机增删 UID。可选“本机管理”使用独立名单。你还没有提供文件地址，当前初始名单仍只有画渣花小烙。

文件示例见 [config/kid-player-creators.example.json](config/kid-player-creators.example.json)，完整接入说明见 [config/README.md](config/README.md)。无需 Mac 常开，也无需先部署专用管理后台。

- 首页按 UP 主头像分类；点头像只显示该作者的影片，支持多个作者、启用/停用和移除。同步成功后使用平台头像与视频封面；图片失败有占位。
- 每位作者分别保存目录、喜欢与观看进度。原作者已有记录保留。仍限每位作者公开合集最新 30 条，不能保证覆盖全部投稿。
- 云端文件整份校验成功才生效；下载或格式错误保留上次名单。空名单明确停用全部作者。在播作者被停用会结束播放。
- 首页使用可复用影片列表，刷新时保留缓存；喜欢操作留在详情，返回保留列表位置。电视使用清晰的焦点边框，放大字体时导航可换行。
- 家庭存储的喜欢和继续观看跨已访问文件夹汇总，并隔离不同连接目录。播放加入音频焦点处理、耳机断开暂停、等待反馈和超时重试。
- 当时调试版本 `0.4.0-sisi` / versionCode 5，沿用包名和调试签名以保留已有数据。真实 VPS 地址和实际华为/TCL 设备仍待接入验收。
- 验证记录：`qa/sisi-0.4.0/development-review.md` 和 `verification.json`。最终 13 项单元测试和 44 项仪器测试全部通过，共 57 项，联网 opt-in 已开启，无跳过。真实音视频解码、暂停、跳转、断网恢复和音频焦点已在专用模拟器验证。

以下旧版本内容是历史记录；当前行为以开头的 0.6 说明为准。

## 0.3 可用的公开合集自动同步（2026-09-04）

当前版本已在 Android 12 专用模拟器完成真实联网同步：从“画渣花小烙”（UID 402576555）的 6 个公开合集读取 184 条视频记录，再单独通过视频详情接口核验作者和公开状态，保留最新 30 条。成功时间写入目录，不再依靠打包的 3 条初始链接。只读取元数据，播放仍直连平台正常返回的音视频流，不下载整部视频归档。

**范围是“公开合集中的视频”，不是“该 UP 主的全部投稿”。未加入合集的新投稿可能遗漏。** 合集已包含本轮主页最新的三条投稿，但这不能证明以后每条都会进入合集。界面在目录、首页与设置中明确标注范围；旧的完整投稿 API 仍受 -352 限制，没有把该限制伪装成已解决。

- 启动/返回时检查，前台每 15 分钟检查，后台由 WorkManager 在有网络时按周期调度；系统省电或强行停止可能延后，不是实时推送。
- 先读取该作者公开合集的所有分页，再去重、按发布日期排序。逐条核验视频的 `owner.mid`，合集里即使放入其他作者的视频也不会加入孩子的目录。
- 最多扫描 20 个合集、500 条合集记录；分页不完整、目录读取中变化、出现不支持的列表类型或平台拒绝时，整次失败并保留上次成功目录，不冒充完整更新。
- 每次只展示核验后的最新 30 条，不是完整历史；喜欢和继续观看不扩大当前目录。
- 请求间隔至少 150ms，有同步总时限和手动请求冷却；不用浏览器登录凭据，不模拟设备指纹，不处理验证码，不使用第三方解析站。
- 同步成功不表示内容经过家长逐条审核。它是作者/来源白名单，不是视频内容识别系统；无弹幕、无评论指不加载平台附加界面，不删改原视频中的内容。

同步实现见 `BiliCollections.java`，检查记录见 `qa/collections-0.3.md`。下面的 0.2 记录保留为历史，不代表当前只能播放三个视频。

0.3 验证：13 项单元测试、22 项模拟器集成测试全部通过（共 35 项），Lint 0 错误、23 项警告，调试 APK 签名校验通过。新增测试验证分页扫描、核验作者、更新时加入新条目、冷却和失败保留旧目录；新条目测试为模拟接口事件，不代表已经等待该作者未来真实发布。

## 0.2 在线订阅原型（历史记录）

点击左侧“UP 主订阅”，订阅用户提供的 UID `402576555`。来源在 `BiliPolicy.java` 固定，应用内不提供修改 UID、搜索、粘贴视频链接、推荐、评论、弹幕或打开外部网页的入口。保持原来的爱心萌可图标和无 PIN 设计。

**自动同步仍受限：** 本轮 Mac 的投稿接口实测返回 -403 / -352；带常规 WBI 签名的投稿请求返回 -352（风控校验失败）。但正常 Chrome 可以加载该作者主页，视频详情和播放资源接口成功返回。主线程没有读取或导出浏览器 Cookie，没有绕过登录或验证码，也不调用第三方解析站。

初始目录包含正常主页最新显示的 3 条真实投稿：汗疱疹、蚊子读评论、游隼。每条都另行通过详情接口核验 `owner.mid=402576555`。来源记录为 2026-09-04；`app/src/main/assets/bilibili-initial.json` 中的 `syncedAt=0` 明确表示不是自动同步成功。App 会显示“初始目录”和同步失败状态，而不是用这三条冒充实时目录。视频里作者自己朗读评论属于视频画面/音轨内容，和应用评论区是两回事；本应用不对原视频内容做删改。

**实际播放已验证一条：** API 31 模拟器中，汗疱疹视频画面持续推进，在 01:50 暂停约 15 秒保持静止，恢复并拖动至 02:30 后继续播放。另两条尚未逐条实播，声音输出未听取验收。平板订阅目录已检查；电视只检查了本次顶部布局，不能据此认定 TCL 真机兼容。详细记录见 `qa/online-notes.md`。

这版先让设备自己维护 JSON 目录，**没有部署电脑常驻后台或云端管理页面**，因此不要求 Mac 常开，也不需要向公网暴露 NAS。尚无成功同步目录时读取打包的初始 JSON；成功后保存到应用私有 SharedPreferences 的 `bili.feed` 字段，包含 schema、uid、syncedAt 和 videos（bvid、uid、title、author、published、duration）。只在完整验证后替换上次成功目录；错误另存，不把失败当作空目录覆盖。

- 同步范围：若接口可用，则同步指定作者最近 30 条投稿，每次替换这个窗口，不是完整历史归档。窗口外影片不再显示，收藏和进度不会扩大白名单。当前只交付初始 3 条，不声称已取得最近 30 条。
- 自动更新：启动/回到应用时检查，前台每 15 分钟检查，后台通过 WorkManager 按 15 分钟周期申请有网络时执行。系统省电、强行停止等可能延后任务，不承诺实时推送。手动请求最少间隔一分钟；遇到平台拒绝停止本次请求，不做绕过或密集重试。
- 播放校验：视频必须在当前允许目录内，播放前再核对视频详情里的作者 UID、视频状态和付费标记。只尝试匿名可访问内容，不处理登录、会员、充电专属或受限预览。
- 纯播放器：使用原生 Media3，按需获取 H.264 视频和 AAC 音轨并合并播放，或使用返回的单段流。没有网页播放器、WebView、评论或弹幕请求；播完停止，不跳到推荐视频。播放地址不持久保存，只使用正常播放缓冲，不做整部下载归档。
- 地址限制：HTTPS，固定 B 站 API 主机，媒体仅允许 `*.bilivideo.com` / `*.bilivideo.cn` 的标准 HTTPS 端口；每一跳媒体重定向都重新检查。会从同一播放接口提供的主/备地址中选择符合限制的地址；未支持的 CDN、格式或清晰度会报错，不转交外部应用。
- 当前能力边界：优先公开 480p 或更低 H.264；多分 P 视频只播放第一 P。封面沿用通用插画，并非真实视频封面。Wi-Fi 和移动网络均会产生正常音视频流量费用。
- 内容边界：自动允许这个作者的新投稿不等于家长逐条审核，视频内自带的商业内容也不会被自动去除。这只是应用内范围限制，不是整台平板的设备级儿童管控。

参考 [RSSHub 的投稿目录设计](https://github.com/DIYgod/RSSHub/blob/master/lib/routes/bilibili/video.ts) 和 [Bilby 的订阅及原生播放思路](https://github.com/NihilDigit/bilby)。本次新增代码为独立实现，未复制其完整客户端或引入账号/指纹模拟流程；这些社区方案不代表平台对第三方播放提供稳定授权或服务保证。

如果正常访问仍持续被平台拒绝，需要先确认可用的合规访问/授权方式，再决定服务端集中同步或其它来源；本版不把这个待解决问题掩盖成已完成。

## 在这台 Mac 上使用

- 双击 `启动kid-player.command`：启动专用 Android 模拟器、安装现有原型并打开。
- 双击 `构建并运行kid-player.command`：重新编译后安装运行，正常更新保留连接设置和观看进度。
- 项目和开发环境均在外置盘，运行前先挂载外置盘。
- 使用命令行即可构建；Android Studio 按需安装，IDE 应选择本机 JDK 17 与外置盘 SDK。
- 先 `source tools/env.sh`；用 `KID_PLAYER_RUNTIME`、`JAVA_HOME`、`ANDROID_HOME` 覆盖本机环境路径，不再提交 Air 的绝对 Java 路径。
- 模拟器是通用 Android ARM64 平板，不是 HarmonyOS 模拟器，也不是 TCL 固件模拟器。电视布局模式仅验证交互布局，不能证明真实 TCL 兼容。
- 默认模拟器是 `KidCinema_Tablet_12`（Android 12 / API 31）。Air 的旧 `KidCinema_Tablet`（Android 11）仅保留历史兼容排查说明，Mini 不建立该 AVD：该 ARM64 镜像复现了[已记录的暂停解码错误](https://github.com/google/ExoPlayer/issues/10576)。启动脚本固定使用新版，不改变真机的解码器选择。

终端运行：

```bash
source tools/env.sh
./gradlew assembleDebug testDebugUnitTest
bash tools/run.sh --build
```

APK 在 `app/build/outputs/apk/debug/app-debug.apk`。这是本地调试签名的原型，不是上架或正式发布版。后续正式分发需固定并妥善保存签名密钥；更换签名不能直接覆盖旧包。

## 原型包含

- 中文影片卡片、单击直接播放、常驻大按钮、收藏、继续观看、当前目录刷新与子文件夹导航。
- 可真正播放的 18 秒本地原创 H.264 演示视频，四张不同封面共用这段视频，界面明确说明；视频无音轨。
- 播放、暂停、进度拖动、前后跳转；离开页面或进入后台保存进度并释放播放器。
- 平板/电视/自动模式。电视使用方向键、确认和返回，焦点显示深紫或白色边框。
- 按用户指定使用爱心萌可主题的生成式角色图标与电视入口图，应用名为“kid player”。这是家庭自用原型素材，不是官方授权标识；若以后公开分发，应另行确认角色授权或换成原创角色。
- 按用户要求不设置儿童密码或家长 PIN；“播放设置”直接打开。SMB 共享本身若需要账号密码，仍需填写连接凭据，并使用 Android Keystore AES-GCM 加密保存，关闭系统备份及设备迁移。
- SMB2/3 读取，支持账号密码、指定共享与相对根目录；不实现写入、删除或其他共享的浏览界面。
- 所有列目录、开文件和播放都经过相对路径检查；拒绝父目录穿越、绝对地址、替代数据流及已识别的符号链接/重解析点。

## 连接华为家庭存储

1. 确认平板/电视与存储处于可互访的同一家庭局域网，不能是隔离的访客网络。
2. 在智慧生活的家庭存储“网络邻居”中查看局域网 IP、共享名及访问凭据。建议在路由器为存储保留固定 IP。
3. 点击“播放设置”，无需任何儿童密码或 PIN。
4. 填入 IP、SMB 端口（通常 445）、共享名（以实机为准）、允许访问的相对目录。例如共享为 `家庭共享`，目录为 `女儿视频`。不要在目录栏填 `smb://` 地址；留空代表允许整个所选共享。
5. 点击“测试此目录的读取”。测试只验证当前目录可读，不验证其他目录不可读。
6. 选择“家庭存储”来源，保存。

请使用存储端已经限制范围的共享凭据，尽量只读。应用界面隐藏其他内容不等于服务器权限隔离；华为设备是否支持所需的单目录/只读粒度，仍须在实机确认。不要配置匿名开放的私人共享，也不要使用全盘管理员凭据。

目录固定为设置中指定的范围，没有儿童密码；任何能进入设置的人都可以修改连接和目录。请勿清除已有正式设备数据来运行测试。

## 验证与边界

0.1 原型验收：9 项路径逻辑测试和 Android 12 专用模拟器上的 5 项集成测试通过；覆盖本地播放、两次 12 秒暂停时进度静止及恢复、收藏、无 PIN 设置、电视焦点、配置加密、SMB 实际播放/读取/跳转和错误凭据拒绝。APK 调试签名校验通过。爱心萌可启动器图标已在模拟器中目视检查；电视布局已检查，但不代表 TCL 真机通过。

2026-09-04 的 0.2 版本：13 项单元测试、16 项模拟器测试通过（共 29 项），Lint 0 错误。新增检查包括作者与 BV 白名单、拒绝混入其他作者、同步被拒时停止、不覆盖旧目录、初始目录不冒充成功同步、播放前核验、HTTPS/重定向/Range 读取、错误页面拒绝和在线失败界面。API 测试使用明确的模拟响应；真实 B 站播放证据单列在上文，不能混为一谈。

纯逻辑测试：

```bash
source tools/env.sh
./gradlew testDebugUnitTest
```

专用模拟器集成测试（仅允许 `KidCinema_Tablet_12`，自动备份并恢复原设置）：

```bash
source tools/env.sh
./gradlew assembleDebug assembleDebugAndroidTest testDebugUnitTest lintDebug
python3 -m venv tools/.venv
tools/.venv/bin/python -m pip install -r tools/requirements.txt
# 首次运行前先通过启动脚本安装并启动应用，让包装脚本可备份初始偏好。
python3 tools/test-emulator.py
# 包含真实平台播放和片尾候选切换的完整回归，会访问 B 站
python3 tools/test-emulator.py --live
```

包装脚本验证 AVD 身份，安装测试 APK，启动临时只读 SMB 测试服务，结束后恢复应用偏好、网络与字体设置并校验哈希。测试服务仅使用项目演示视频，不共享其他本地文件夹。请勿直接对家庭正式设备运行仪器测试。

尚需实机验收：华为 SMB 协商及账户权限、MatePad 的系统解码/休眠、TCL 当前固件安装/启动入口/遥控器、真实片源的音轨/字幕/高码率/HDR。原型没有转码功能，也不承诺播放所有扩展名对应的编码。SMB 收藏和继续观看汇总当前连接范围内已访问文件夹；不同设备之间不做进度同步。

应用不是设备级儿童锁，不能阻止退出后使用其他软件。尚未做完整安全审计，不应把本原型当作唯一的儿童内容隔离措施。

官方资料核对：华为的[家庭存储网络邻居说明](https://consumer.huawei.com/cn/support/content/zh-cn16069288/)确认第三方 SMB 客户端可以访问共享内容。TCL 的[通用 APK 安装说明](https://www.tcl.com/by/be/support-tv/faq/q1770712000586399)并非中国区 65Q10L Pro 型号专属说明，不能据此保证该电视当前固件允许安装。仍需在电视“关于本机”核对系统版本，并实际测试 APK 安装；不要刷机或关闭安全功能来绕过限制。

## 代码位置

- `MainActivity.java`：首页、卡片、家长配置和两种布局。
- `PlayerActivity.java`：Media3 播放和生命周期。
- `SmbLibrary.java` / `SmbDataSource.java`：只读 SMB 浏览与随机位置读取。
- `PathPolicy.java`：统一目录边界。
- `AppStore.java`：配置加密、进度和喜欢。
- `ArtView.java`：本地演示封面。`RemoteImages.java`：平台头像和缩略图缓存。
- `RemoteConfig.java`：HTTPS 云端名单读取、完整校验和更新。
- `tools/generate-demo.swift`：原创演示视频生成脚本。

第三方组件：AndroidX Media3、SMBJ（Apache-2.0）、Bouncy Castle、SLF4J，以及仅用于测试的 Impacket。依赖版本固定在构建文件中；正式交付前应再次核验许可证和安全更新。

## 名称、历史证据与未来路径

显示名称精确为 `kid player`，仓库、脚本和新交付产物 slug 为 `kid-player`。旧 `sisi-cinema` ZIP、历史 APK、历史版本号及 `qa/sisi-*` 引用保留原名和原 SHA，仅作历史证据。历史 QA 不在 Git 源码中，须从交接归档取得；本次接收状态见 `docs/handoff/RECEIPT-MAC-MINI.md`。

`applicationId=family.kidcinema`、存储键保持不变。Mini 的新调试签名不能覆盖 Air 的旧调试签名包，不得卸载清数据冒充升级成功。`bash tools/package.sh` 将调试构建导出至 `output/releases/kid-player-<version>-debug.apk`；Gradle 内部中间输出仍使用标准 `app-debug.apk`。

更新源现已使用 `kid-player.shiyu.ren/kid-player/update.json` 与版本化 APK；作者名单仍独立配置。当前 GitHub 用于私人源码和交接归档，未来可切换到匿名可访问的 HTTPS 分发源，详见应用更新说明。
