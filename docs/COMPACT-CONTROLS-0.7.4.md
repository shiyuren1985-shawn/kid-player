# 0.7.4 紧凑播放按钮

日期：2026-09-08。版本 0.7.4 / code 15。

用户要求将播放页底部四个按钮高度减半。按钮最小高度由 112 dp 调整为 56 dp，图标由上方 40 dp 改为左侧 28 dp，字体 18 sp，上下内边距 8 dp。保留按钮文字、底色、状态切换、无障碍描述和遥控焦点；不调整结束页重放或失败重试按钮。

## 验证与发布

- PASS：assembleDebug、assembleDebugAndroidTest、lintDebug；Lint 0 错误、44 条既有警告。
- PASS：专用 AVD 播放器回归和隔离三态视觉，wrapper 报告 OK 7（45.818 秒）；6 项本地执行通过（包含临时视觉测试），1 项真实网络 opt-in 跳过。测试源精确恢复，偏好恢复确认。
- PASS：播放、暂停且已收藏、遥控焦点三态均无截断。320 dpi 平板四个按钮实测均为 112 px，即 56 dp，符合减半要求。报告 qa/compact-controls-0.7.4/visual-review.md。
- PASS：原子发布并同步 launchd 服务镜像，公网清单与本地发布 JSON 一致，版本 0.7.4 / code 15；APK HEAD HTTP 200、长度 13,353,433 字节一致，签名保持不变。
- APK SHA256：0187d3c882d902568d605ea90165b204322f123028215b936d077ae095258d9a。
- 未在家庭平板安装验收，未重复完整公网 APK 下载。专用 AVD 完成后关闭。

